/// Backwards-compatible import path for product-detail dose safety.
///
/// The implementation lives in `services/health/dose_safety.dart` so product
/// detail, health facts, stack aggregation, and warning gates share one
/// clinical interpretation boundary.
library;

export 'package:pharmaguide/services/health/dose_safety.dart';
